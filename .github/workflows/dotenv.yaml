name: dotenv

on:
  push:
    paths:
      - 'dotenv/**'
      - '.github/workflows/dotenv.yaml'
    tags:
      - 'dotenv/v*'
    branches:
      - '**'
  pull_request:
    paths:
      - 'dotenv/**'
      - '.github/workflows/dotenv.yaml'

  schedule:
    - cron: '29 2 * * 0' # Weekly

env:
  GOVERSION: '1.22.x'
  GOLANGCI_LINT_VERSION: v2.11.4

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: ./dotenv
    steps:
      - name: 🟣 Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GOVERSION }}

      - name: 🧳 Checkout
        uses: actions/checkout@v4

      - name: 🔃 Restore
        run: go mod tidy

      - name: 🏭 Build
        run: go build -v ./...

      - name: 🧪 Test
        run: go test -v ./...

  lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./dotenv
    steps:
      - name: 🧳 Checkout
        uses: actions/checkout@v4

      - name: 🟣 Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GOVERSION }}

      - name: 🧹 golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: ${{ env.GOLANGCI_LINT_VERSION }}
          working-directory: dotenv

  vuln:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./dotenv
    steps:
      - name: 🧳 Checkout
        uses: actions/checkout@v4

      - name: 🟣 Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GOVERSION }}

      - name: 🔐 govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

  release:
    name: GitHub Release
    if: startsWith(github.ref, 'refs/tags/dotenv/')
    needs: [build, lint, vuln]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 🧳 Checkout
        uses: actions/checkout@v4

      - name: 📣 Create release with generated notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ver="${GITHUB_REF_NAME#dotenv/}"
          gh release create "${GITHUB_REF_NAME}" \
            --generate-notes \
            --title "dotenv ${ver}"
